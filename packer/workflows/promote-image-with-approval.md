# Workflow: Promote Image with Approval

1. Gather evidence: build manifest, SBOM, scan report, change log
2. Request approval from approvers group
3. On approval, promote in HCP to target channel(s)
4. Notify downstream consumers (Terraform modules, autoscaling groups)